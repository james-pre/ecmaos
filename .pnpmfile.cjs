function afterAllResolved(lockfile, context) {
  // Placeholder
  return lockfile
}

function readPackage(pkg, context) {
  // Placeholder
  return pkg
}

module.exports = {
  hooks: {
    afterAllResolved,
    readPackage
  }
}
